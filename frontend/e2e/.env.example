# Playwright E2E 測試帳號設定範本
#
# 使用方式：複製本檔為同目錄下的 `.env`，填入實際帳密。
#   copy .env.example .env
#
# `.env` 已被專案根目錄 .gitignore 涵蓋，不會進版控。
# 切勿把實際帳密寫進 .env.example 或任何 .ts 測試檔。

# 測試目標站台（預設 http://127.0.0.1:5300）
E2E_BASE_URL=http://127.0.0.1:5300

# 測試帳號（帳號或 Email 皆可，對應登入表單的 identifier 欄位）
E2E_USER=
E2E_PASS=
